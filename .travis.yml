language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t tarun818/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true tarun818/docker-react npm run test
